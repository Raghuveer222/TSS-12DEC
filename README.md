Summary

The app handles routes for logging in, signing up, and viewing user or transporter pages.

It uses MongoDB to store user, transporter, and shipment data.

It uses EJS for rendering dynamic views based on the user or transporter data.

The server listens on port 3000 and connects to a local MongoDB database named finaltss.


step 1:
rendering only index.ejs file 



